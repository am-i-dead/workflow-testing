on:
    workflow_dispatch:
      inputs:
        message:
          required: true
        world:
          type: choice
          options:
            - minecraft
            - earth

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: echo
        run: ls -al

      - name: builder
        run: ./builder/build.sh 